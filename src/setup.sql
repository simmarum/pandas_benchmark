CREATE TABLE res (
    id INT NOT NULL AUTO_INCREMENT,
    ts TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    time INT NOT NULL,
    cpu INT NOT NULL,
    mem INT NOT NULL,
    avg_pt INT NOT NULL,
    PRIMARY KEY (id)
);

ALTER TABLE
    res
ADD
    COLUMN ver INT;

UPDATE
    res
SET
    ver = 1
WHERE
    ver IS NULL;